kind: Service
apiVersion: v1
metadata:
  # Unique key of the Service instance
  name: fleetman-webapp
spec:
  ports:
    - name: http
      port: 80
      targetPort: 30080
  selector:
    app: webapp 
  type: NodePort

