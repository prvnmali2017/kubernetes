apiVersion: extensions/v1beta1
kind: DaemonSet
metadata:
  name: rs-one
spec:
  template:
    metadata:
      labels:
        system: ReplicaOne
    spec:
      containers:
      - name: nginx
        image: nginx:1.9.1
        ports:
        - containerPort: 80
